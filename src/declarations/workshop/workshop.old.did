type State = 
 record {
   events: List;
   value: nat;
 };
type List = 
 opt record {
       Event;
       List;
     };
type Event = variant {initEvent: record {value: nat;};};
type Command = variant {initCommand: record {value: nat;};};
service : {
  action: (Command) -> (opt State);
}
